# ADR-0014: Identity authority is structural

Status: Accepted

## Decision

The configured handle and display name flow together into every authorized control action. No caller or call target is inferred from ambient UI state.

## Enforcement

Required config fields, exact semantic matching, and fail-closed native actions.
